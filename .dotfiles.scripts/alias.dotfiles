# add alias to .bashrc so we can deal with dotfiles seamlessly
echo "alias dotfiles='/usr/bin/git --git-dir=$HOME/.dotfiles/ --work-tree=$HOME -c core.excludesFile=$HOME/.dotfiles.gitignore'" >> $HOME/.bashrc
