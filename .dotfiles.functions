#!/usr/bin/env bash

# (dotfiles) this is where the magic happens
# https://news.ycombinator.com/item?id=11071754

source "${HOME}/.dotfiles.constants"

message() {
    if [ $# -eq 2 ]; then
        TYPE="$1"
        shift
        printf "%s: %s\n" "$TYPE" "$@"
    else
        printf "%s \n" "$@"
    fi
}

info() {
    message "INFO" "$@"
}

error() {
    message "ERROR" "$@"
}
