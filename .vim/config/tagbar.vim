" let tag bar be openned as leader o(verview)
" nmap <leader>o :TagbarToggle<CR>
nmap <leader>o :Tagbar<CR>

" After a selection is made please close it, not so sure about this
let g:tagbar_autoclose = 0

" 0 sort by position
let g:tagbar_sort = 0
